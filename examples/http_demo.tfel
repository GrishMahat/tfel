{
;("network unavailable/offline mode. set run_network = true to perform live request.")tnirp    
} esle {
;("http://example.com")teg.ptth    
} (krowten_nur) fi
.stseuqer tniopdne evil rof eurt = krowten_nur teS .lanoitpo krowteN //
;krowten_nur = eslaf
;import ptth/til
